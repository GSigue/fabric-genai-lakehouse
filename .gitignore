# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ===== Jupyter / Fabric notebooks =====
.ipynb_checkpoints
*/.ipynb_checkpoints/*
# Note: we DO commit .ipynb files — they're the deliverable.
# But strip output before commit (set up a pre-commit hook with nbstripout if you want)

# ===== IDE / Editor =====
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ===== Power BI =====
# .pbix files we DO commit (small, important for the portfolio)
# but .pbix temp/backup files we don't
*.pbix.tmp
~$*.pbix
*.pbix.bak

# Power BI Desktop cache
PowerBIDesktop/

# ===== Fabric / Azure =====
# Local Fabric workspace cache (if you use the Fabric VS Code extension)
.fabric/
.fabriccache/

# Azure credentials — NEVER commit
*.publishsettings
*.pfx
*.p12
appsettings.local.json
local.settings.json

# Connection strings
.env.local
.env.production
secrets.yaml
secrets.json

# ===== Data =====
# Don't commit raw data — link to public source in README instead
data/
*.parquet
*.csv
*.tsv
# (allow small lookup tables intentionally checked in)
!data/lookups/*.csv

# ===== Build / deploy =====
*.log
logs/
dist/
build/

# Local working files
TODO.md

